<% if flash[:success] %>
  <div class="flash">
    <%= flash[:success] %>
  </div>
<% elsif flash[:alert] %>
  <div class="flash">
    <%= flash[:alert] %>
  </div>
<% end %>
<br>
<%= link_to "VOIR MON PANIER", cart_path(current_user.id) %>
<br><br>
<%= link_to "Voir la liste de mes commandes", orders_path %>


<br><br>

<div class="index-card d-flex justify-content-center flex-wrap">

<% @items.each do |item| %>

  <div class="card">
  <img src="<%= item.image_url %>" class="card-img-top" alt="kitten-img">
  <div class="title-card up">
    <%= link_to "#{item.title}", item_path(item.id) %>  </div>
  <div class="price-index up">
  <%= item.price %> €
  </div>
  <a href="#" style="color:var(--dark-grey);">
 
  <%= link_to cart_path(item.id), method: :put do %>
    <div class="add-to-cart">
      <i class="fas fa-shopping-cart"></i>
    </div>
  <% end %>

</div>  

<% end %>

</div>
