
    <% @items.each do |item| %>

        <div class="card">
          <%= image_tag item.image_url, :class => "card-img-top" %>
    
          <div class="title-card up">
            <%= link_to "#{item.title}", item_path(item.id) %>
          </div>
    
          <div class="price-index up">
          <%= item.price %> €
          </div>
    
          <%= link_to cart_path(item.id), method: :put, :remote => true do %>
            <div class="add-to-cart">
              <i class="fas fa-shopping-cart"></i>
            </div>
          <% end %>
    
        </div>  
    
      <% end %>